package com.example.compose2extended.view

/*
import androidx.compose.foundation.layout.Column
import androidx.compose.foundation.layout.fillMaxSize
import androidx.compose.foundation.layout.padding
import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.compose.ui.text.font.FontWeight
import androidx.compose.ui.unit.dp
import com.example.compose2extended.data.model.MovieResult

@Composable
fun MovieDetailsScreen(movie: MovieResult) {
    // Display movie details using the passed MovieResult object
    // You can customize this based on your UI requirements
    // Example: Show movie title, release date, etc.
    Column(
        modifier = Modifier
            .fillMaxSize()
            .padding(16.dp)
    ) {
        Text(text = "Title: ${movie.original_title}", fontWeight = FontWeight.Bold)
        Text(text = "Release Date: ${movie.release_date}")
        // Add more details as needed
    }
}*/
