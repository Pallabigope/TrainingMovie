package com.example.compose2extended.repository

/*
import com.example.compose2extended.network.ProductService


class ProductRepository ( val productService: ProductService) {

    suspend fun getProducts(): List<Product>? {
        val response: Response<List<Product>>
        response = productService.getProducts()

        if (response.isSuccessful) {
            return response.body()
        } else {
            // Handle error, e.g., log or throw an exception

        }
    }
}*/
